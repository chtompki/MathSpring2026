%! Author = chtompki
%! Date = 1/14/26
% Example LaTeX document for GP111 - note % sign indicates a comment
\documentclass[12pt]{amsart}
% Default margins are too wide all the way around. I reset them here
\setlength{\hoffset}{-.75in}
\setlength{\textwidth}{6.5in}
\setlength{\voffset}{-.5in}
\setlength{\textheight}{9.0in}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{pgfplots}
\usepackage{tikz}
\usepackage{amsthm}
\usepackage{enumitem}
\usepackage{setspace}

\newenvironment{solution}
{\begin{proof}[Solution]}
{\end{proof}}

\title{Advanced Linear Algebra\\Homework 4}
\author{Rob Tompkins}
\date{\today}

\begin{document}
    \maketitle
    \date{\today}
    \begin{onehalfspace}

        % Problem P.2.9
        \noindent\textbf{P.2.9} Let $\mathbf{u} = [\begin{matrix}1 & 0& 1\end{matrix}]^\top$,
        $\mathbf{v}_1=[\begin{matrix}0 & 1+i& 1-i\end{matrix}]^\top$,
        $\mathbf{v}_2=[\begin{matrix}2i & 1& 0\end{matrix}]^\top$, and
        $\mathbf{v}_3=[\begin{matrix}2 & -1& 1\end{matrix}]^\top$.
        Let $\beta=\mathbf{v}_1,\mathbf{v}_2,\mathbf{v}_3$.
        Show that $\beta$ is a basis for $\mathbb{C}^3$ and compute $[\mathbf{v}]_\beta$.
        \begin{proof}
            Sol
        \end{proof}

        % Problem P.2.11
        \noindent\textbf{P.2.11} Compute $\text{rank}(A)$, in which
        \begin{displaymath}
            A = \left[\begin{matrix}
                0 & 3 & 1 & 5 & 0 & 5 & 1 & 4 & 4 & 0\\
                2 & 3 & 0 & 0 & 1 & 3 & 3 & 2 & 1 & 5\\
                1 & 4 & 0 & 1 & 5 & 3 & 3 & 0 & 0 & 0\\
                2 & 4 & 5 & 0 & 2 & 0 & 2 & 0 & 4 & 3
            \end{matrix}\right] \in \mathbf{M}_{4\times 10}.
        \end{displaymath}
        \emph{Hint.} Find four columns whose span includes $\mathbf{e}_1, \mathbf{e}_2, \mathbf{e}_3, \mathbf{e}_4$.
        \begin{solution}
            Sol
        \end{solution}

        % Problem P.2.16
        \noindent\textbf{P.2.16} Let $\beta=\mathbf{u}_1, \mathbf{u}_2, \ldots,\mathbf{u}_n$ be a basis for a non-zero
        $\mathbb{F}$-vector space $\mathcal{V}$.
        (a) If any vector in $\mathcal{V}$ is appended to $\beta$, explain why the resulting list still spans
        $\mathcal{V}$ but is not linearly independent.
        (b) If any vector in $\beta$ is omitted, explain why the resulting list will still be linearly independent but
        no longer spans $\mathcal{V}$.
        \begin{solution}
            Sol
        \end{solution}

        % Problem P.2.18
        \noindent\textbf{P.2.18} Let $\mathcal{V}$ be an $n$-dimensional $\mathbb{F}$-vector space with $n\ge 2$ and let
        $\beta = \mathbf{v}_1, \mathbf{v}_2, \ldots, \mathbf{v}_r$ be a list of vectors in $\mathcal{V}$ with
        $1\le r< n$.
        Show that $\beta$ does not span $\mathcal{V}$.
        \begin{proof}
            Sol
        \end{proof}

        % Problem P.2.41
        \noindent\textbf{P.2.41} What is the rank of
        \begin{displaymath}
            A = \left[\begin{matrix}
                          1 & n+1 & 2n+1 & \cdots & (n-1)(n+1)\\
                          2 & n+2 & 2n+2 & \cdots & (n-1)(n+2)\\
                          \vdots & \vdots & \vdots & \ddots & \vdots\\
                          n & 2n & 3n & \cdots & n^2
            \end{matrix}\right]?
        \end{displaymath}
        Find a basis for $\text{col}(A)$.
        \begin{solution}
            Sol
        \end{solution}

        % Problem P.3.2
        \noindent\textbf{P.3.2} Let
        \begin{displaymath}
            A = \left[\begin{array}{c|cc}
                          -1 & 2 & 3\\
                          \hline
                          2 & -3 & 1\\
                          3 & 1 & -2
            \end{array}\right] =
            \left[\begin{array}{cc|c}
                          -1 & 2 & 3\\
                          \hline
                          2 & -3 & 1\\
                          3 & 1 & -2
            \end{array}\right]
            \qquad\text{and}\qquad
            B = \left[\begin{matrix}
                          1 & 3 & -2\\
                          3 & -2 & 1\\
                          -2 & 1 & 3
            \end{matrix}\right]
        \end{displaymath}
        (a) Compute $AB$ in two different ways by partitioning $B$ conformally with each presentation of $A$ and then
        by preforming block matrix multiplication.
        (b) Compute $AB$ in standard manner, and verify that all three answers agree
        \begin{solution}
            Sol
        \end{solution}

        % Problem P.3.3
        \noindent\textbf{P.3.3} Let
        \begin{displaymath}
            A = \left[\begin{array}{c|cc|cc}
                          a_{11} & a_{12} & a_{13} & a_{14} & a_{15} \\
                          \hline
                          a_{21} & a_{22} & a_{23} & a_{24} & a_{25} \\
                          a_{31} & a_{32} & a_{33} & a_{34} & a_{35} \\
                          \hline
                          a_{41} & a_{42} & a_{43} & a_{44} & a_{45}
            \end{array}\right]
            \qquad\text{and}\qquad
            B = \left[\begin{matrix}
                          b_{11} & b_{12} & b_{13} \\
                          b_{21} & b_{22} & b_{23} \\
                          b_{31} & b_{32} & b_{33} \\
                          b_{41} & b_{42} & b_{43} \\
                          b_{51} & b_{52} & b_{53} \\
            \end{matrix}\right].
        \end{displaymath}
        How should $B$ be partitioned to compute $AB$ with block matrices.
        \begin{solution}
            Sol
        \end{solution}

    \end{onehalfspace}
\end{document}
